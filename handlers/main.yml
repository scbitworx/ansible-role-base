---
- name: Base | reload sshd
  ansible.builtin.service:
    name: "{{ __base_sshd_service }}"
    state: reloaded
  become: true

- name: Base | reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true
