# {{ ansible_managed }}
# ~/.config/bash/inputrc — managed by ansible-role-base

# Case-insensitive tab completion
set completion-ignore-case on

# Show all matches at once if ambiguous
set show-all-if-ambiguous on

# Append slash to completed directory names
set mark-directories on

# Color files by type during completion
set colored-stats on

# Treat hyphens and underscores as equivalent in completion
set completion-map-case on
