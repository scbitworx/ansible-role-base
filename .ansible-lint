---
profile: production

strict: true

extra_vars:
  base_users:
    - name: lint_mock_user

mock_roles: []

exclude_paths:
  - .github/
  - molecule/

enable_list:
  - fqcn
  - no-changed-when
  - no-handler
  - yaml

warn_list:
  - role-name[match]

skip_list: []
